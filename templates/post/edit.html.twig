{% extends 'base.html.twig' %}

{% block title %}Modifier l'article
{% endblock %}

{% block body %}
    <h1>Modifier l'article</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="hidden" name="_token" value="{{ csrf_token('edit' ~ (post is defined ? post.id : '')) }}">
    <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
    {{ form_end(form) }}

    <a href="{{ path('post_index') }}" class="btn btn-secondary">Retour à la liste des articles</a>
{% endblock %}